import { Layout } from "antd";

const contentStyle: React.CSSProperties = {
  textAlign: "center",
  minHeight: "calc(100vh - 140px)",
  lineHeight: "120px",
  color: "#fff",
  backgroundColor: "#70737A",
};

const HistoryPage = () => {
  return <Layout.Content style={contentStyle}>Pev!!</Layout.Content>;
};

export default HistoryPage;
